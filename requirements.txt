# =============================================================================
# LLM-Lite Underwriter API - 통합 의존성 (Google Cloud Run용)
# =============================================================================

# Core API Framework
fastapi>=0.104.0,<0.105.0
uvicorn[standard]>=0.24.0,<0.25.0
gunicorn>=21.2.0  # 프로덕션용 WSGI 서버
pydantic[email]>=2.5.0,<3.0.0
pydantic-settings>=2.4.0
python-multipart>=0.0.6

# LangChain & LangGraph (AI 에이전트 시스템)
langgraph>=0.0.60
langchain>=0.1.0
langchain-core>=0.1.0
langchain-openai>=0.1.0
langchain-community>=0.0.20

# HTTP & Async
httpx>=0.25.0,<0.26.0
requests>=2.31.0
aiofiles>=23.0.0

# 데이터 처리 & 수치 계산 (인수심사 엔진)
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# 로깅 & 모니터링
structlog>=23.0.0  # 구조화된 로깅
psutil>=5.9.6      # 시스템 메트릭스

# 보안 & 인증
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Redis (선택적 - 분산 태스크 관리)
redis[hiredis]>=5.0.0,<6.0.0

# 타입 힌트
typing-extensions>=4.0.0

# 환경 관리
python-dotenv>=1.0.0

# Firebase (선택적)
firebase-admin>=6.0.0

# 프로덕션 의존성
prometheus-client>=0.19.0  # 메트릭스 수집

# =============================================================================
# 개발 의존성 (선택적 - 프로덕션에서는 제외 가능)
# =============================================================================

# 테스트
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0

# 코드 품질
black>=23.10.0
isort>=5.12.0
flake8>=6.1.0

# =============================================================================
# 클라우드 런 최적화 설정
# =============================================================================
# - 빠른 시작을 위한 경량화된 의존성
# - 캐시 활용을 위한 버전 고정
# - 메모리 효율성을 고려한 패키지 선택